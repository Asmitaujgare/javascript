let birthYear = 1996;
let futureYear = 2025;
let possibleAge = futureYear - birthYear;
console.log(`If you were born in ${birthYear}, then in ${futureYear} you'll be ${possibleAge} or ${possibleAge + 1} (depending on the month in ${futureYear}).`);